# 검증 기록 — 2026-09-11

## 확인된 것

- Scholar Search MCP 원본 다운로드: commit `1392e7e6f483bb2fdbef42e37610b82620612ee7`.
- PaperQA 원본 다운로드: `v2026.08.12`, commit `57e89f7223b0960d5ee5ea048c69e3c47e088572`.
- 프로젝트 전용 Windows Python 3.12 가상환경에 MCP 의존성과 PaperQA 2026.8.12 설치.
- 단위·연결 테스트: 참고문헌 중복, 출발 논문 중복, 순환, 경로, 상한, DOI 정규화, 파일 덮어쓰기 차단, 트랙 구분, 페이지 순회, 사설 URL 차단, 모델 실행 허용 게이트.
- 실제 stdio MCP 초기화·도구 6개 열거·승인 전 분석 차단 확인.
- 공개 API 실제 확인: `ARXIV:2409.13740` 논문 상세 조회 및 참고문헌 최대 3건 조회 모두 HTTP 200. 3건 제한에 따라 `truncated=true` 보고. 이 논문은 연결 시험용이지 선정된 연구 논문이 아님.
- 원 응답: `runs/ai/20260911T031749-70ce1308a9bc/`.
- PaperQA import, Settings 생성, aadd/aquery 호출 인자 호환성 확인. 사용자 홈의 기본 캐시 경로 권한 문제를 발견하여 캐시를 프로젝트 데이터 폴더로 한정한 뒤 재검사 통과.
- `pip check`: 의존성 충돌 없음. 운영 스킬 구조 검사 통과.

## 아직 확인하지 않은 것

- 실제 Hermes 실행 호스트·버전·등록 위치: 미확인. 사용자는 디스코드로 대화함을 확인했으나 디스코드가 실행 서버는 아님.
- 실제 디스코드 → Hermes → 이 MCP 서버 호출: 미실행.
- 실제 모델을 이용한 PaperQA 분석 품질·비용: 미실행. 기존 자격증명을 탐색하거나 유료 호출하지 않음. 모의 모델 응답을 이용한 연결 테스트는 실제 분석 품질 검증이 아님.
- 실제 PDF 다운로드·파싱 end-to-end: 미실행. PDF 보존/해시와 호스트 차단은 테스트 자료로 검사.
- 실제 여러 출발 논문의 전체 루트 그래프 품질: 미검증. 공통 경로 계산은 모의 그래프 검증, 실제 API는 한 논문 소량 응답 검증으로 분리.
- 공식 게재·BK21 기준선·루트논문 원천성: 자동 확정하지 않음. 검토자가 출처 근거를 기록해야 함.

## 재검사

```text
python -m unittest discover -s tests -v
python scripts/smoke_mcp.py
python scripts/check_paperqa.py
python -m pip check
```

`scripts/live_scholar_check.py`는 공개 API에 실제 요청한다. 테스트가 성공해도 DB 참고문헌의 완전성·원문 신원·인용 이유가 모두 검증되는 것은 아니다.

이 구현은 초기 MCP 프로그램과 운영 스킬이며 전체 연구 파이프라인 배포 완료가 아니다. 기존 연구 문서·결과·Hermes/디스코드 설정은 변경하지 않았다.
